extends layout

block content
  if user  
    h1 Welcome back #{user.username}
    a(href='/users/log-out') Log Out
  else 
    h1 You are not logged in. Please log in
    h1 
      a(href='users/log-in') Here